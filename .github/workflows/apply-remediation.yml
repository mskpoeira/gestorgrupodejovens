name: Validar e consolidar remediacao SGJ

on:
  push:
    branches: [fix/auditoria-final-20260914]
    paths:
      - '.github/workflows/apply-remediation.yml'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  validate-and-consolidate:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout da branch
        uses: actions/checkout@v4
        with:
          ref: fix/auditoria-final-20260914
          fetch-depth: 0

      - name: Recompor remediacao e corrigir icone Tauri
        shell: bash
        run: |
          cat .github/remediation/*.txt | base64 -d > /tmp/sgj-v03-remediation.tar.gz
          tar -xzf /tmp/sgj-v03-remediation.tar.gz -C .
          python3 - <<'PY'
          import binascii
          import struct
          import zlib
          from pathlib import Path

          width = height = 512
          rgba = bytes((27, 78, 115, 255))
          raw = b''.join(b'\x00' + rgba * width for _ in range(height))

          def chunk(kind: bytes, data: bytes) -> bytes:
              body = kind + data
              return struct.pack('>I', len(data)) + body + struct.pack('>I', binascii.crc32(body) & 0xffffffff)

          png = (
              b'\x89PNG\r\n\x1a\n'
              + chunk(b'IHDR', struct.pack('>IIBBBBB', width, height, 8, 6, 0, 0, 0))
              + chunk(b'IDAT', zlib.compress(raw, 9))
              + chunk(b'IEND', b'')
          )
          target = Path('apps/desktop/src-tauri/icons/icon.png')
          target.parent.mkdir(parents=True, exist_ok=True)
          target.write_bytes(png)
          PY

      - name: Node.js 22
        uses: actions/setup-node@v4
        with:
          node-version: 22

      - name: Rust stable
        uses: dtolnay/rust-toolchain@stable

      - name: Dependencias Linux oficiais do Tauri
        shell: bash
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            libwebkit2gtk-4.1-dev \
            build-essential \
            curl \
            wget \
            file \
            libxdo-dev \
            libssl-dev \
            libayatana-appindicator3-dev \
            librsvg2-dev

      - name: Atualizar Vitest para versao corrigida
        run: npm pkg set devDependencies.vitest="^5.0.0" --workspace apps/web

      - name: Gerar lockfiles reproduziveis
        shell: bash
        run: |
          npm install --package-lock-only --ignore-scripts
          cd apps/desktop/src-tauri
          cargo generate-lockfile

      - name: Instalar dependencias reproduziveis
        run: npm ci

      - name: Testes automatizados
        run: npm test

      - name: Auditoria de dependencias npm
        run: npm audit --audit-level=moderate

      - name: Build Web e typecheck
        run: npm run build

      - name: Validar Rust SQLite/Tauri
        working-directory: apps/desktop/src-tauri
        run: cargo check --locked

      - name: Limpar artefatos temporarios
        shell: bash
        run: |
          rm -rf .github/remediation
          rm -f docs/_cargo_check.log
          rm -f docs/_temp_branch_marker.md docs/_branch_v0.3.txt docs/_branch_v0.3.ready docs/_branch_v0.3.final docs/_branch_v0.3.marker

      - name: Consolidar remediacao validada
        shell: bash
        run: |
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add -A
          git commit -m 'fix: remediar auditoria e estabilizar SGJ v0.3.0'
          git push origin HEAD:fix/auditoria-final-20260914
